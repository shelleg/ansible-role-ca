---
# defaults file for ansible-role-ca
ca_certs_dir: /usr/share/ca-certificates/
ca_rootca_password: Chang3m3

#ca_country: "{{ shellg.ca.country | default('IL') }}"
#ca_state: "{{ shellg.ca.state | default('Israel') }}"
#ca_locality: "{{ shellg.ca.locality | default('Israel') }}"
#ca_organization: "{{ shelleg.domain_prefix | default('domain.local') }}"
#ca_organizationalunit: "{{ shellg.ca.organizationalunit | default('IT') }}"
#ca_email: "admin-at-{{ shelleg.domain_prefix | default('domain.local') }}"

ca_country: IL
ca_state: Israel
ca_locality: Israel
ca_organization: "{{ shelleg.domain_prefix | default('domain.local') }}"
ca_organizationalunit: IT
ca_commonname: "{{ shelleg.domain_prefix | default('domain.local') }}"
ca_email: "admin-at-{{ shelleg.domain_prefix | default('domain.local') }}"





